{% set delay = signals.delay ?? 100 %}
{% set message = 'Hello, world!' %}

{% for i in range(0, message|length - 1) %}
    {% patchelements %}
        <div id="message">
            {{ message|slice(0, i + 1) }}
        </div>
    {% endpatchelements %}

    {# Sleeps for the provided duration. Provided by a custom Twig extension function. #}
    {% sleep delay ms %}
{% endfor %}
